# 📂 Category: GENERAL CODING WORKFLOWS

This section details the extensive use cases and training materials for the `GENERAL CODING WORKFLOWS` domain.

---

## 🛠️ Skill: `arch-aep-runner`

**Description:** Manage the ARCH-AEP workflow review cycle.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `arch-aep-runner` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `arch-aep-runner`.
3. **Orchestration**: As part of a larger multi-agent sequence where `arch-aep-runner` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **arch-aep-runner** workflow to accomplish this task."*
> *"Please load the **arch-aep-runner** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **arch-aep-runner**."*

---

## 🛠️ Skill: `docs-architect`

**Description:** Execute a Gold Standard documentation overhaul or normalize cross-links across the existing docs suite.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `docs-architect` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `docs-architect`.
3. **Orchestration**: As part of a larger multi-agent sequence where `docs-architect` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **docs-architect** workflow to accomplish this task."*
> *"Please load the **docs-architect** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **docs-architect**."*

---

## 🛠️ Skill: `implementation-session`

**Description:** Start an implementation cycle: review priorities, check stale branches, orchestrate agents for coding tasks, and wrap up the session.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `implementation-session` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `implementation-session`.
3. **Orchestration**: As part of a larger multi-agent sequence where `implementation-session` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **implementation-session** workflow to accomplish this task."*
> *"Please load the **implementation-session** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **implementation-session**."*

---

## 🛠️ Skill: `planning-with-files`

**Description:** Implements Manus-style file-based planning for complex tasks. Creates task_plan.md, findings.md, and progress.md. Use when starting complex multi-step tasks, research projects, or any task requiring >5 tool calls. Now with automatic session recovery after /clear.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `planning-with-files` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `planning-with-files`.
3. **Orchestration**: As part of a larger multi-agent sequence where `planning-with-files` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **planning-with-files** workflow to accomplish this task."*
> *"Please load the **planning-with-files** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **planning-with-files**."*

---

## 🛠️ Skill: `pr-manager`

**Description:** Review, test, lint, and prepare smart merges for all open and closed PRs, verifying features and documenting technical debt.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `pr-manager` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `pr-manager`.
3. **Orchestration**: As part of a larger multi-agent sequence where `pr-manager` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **pr-manager** workflow to accomplish this task."*
> *"Please load the **pr-manager** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **pr-manager**."*

---

## 🛠️ Skill: `repo-ingestor`

**Description:** Ingest external repositories, research papers, and benchmarks using 40-agent swarms to analyze, adapt features, and improve current workflows.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `repo-ingestor` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `repo-ingestor`.
3. **Orchestration**: As part of a larger multi-agent sequence where `repo-ingestor` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **repo-ingestor** workflow to accomplish this task."*
> *"Please load the **repo-ingestor** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **repo-ingestor**."*

---

## 🛠️ Skill: `session-wrap`

**Description:** Wrap up the current coding session by creating PRs, updating the session log, preparing the next session md, and updating the claude md with learnings.

### 🧠 Core Directives & Framework
*No specific directives found.*


#### 🎯 Primary Use Cases
1. **Direct Execution**: When you need to immediately apply the `session-wrap` workflow to the current task.
2. **Consultation**: When asking the AI to review your existing architecture against the principles defined in `session-wrap`.
3. **Orchestration**: As part of a larger multi-agent sequence where `session-wrap` handles a specific specialized sub-task.

#### 💡 Training & Invocation Examples
To trigger this skill in your AI assistant, use explicit phrasing:
> *"Adopt the **session-wrap** workflow to accomplish this task."*
> *"Please load the **session-wrap** skill and evaluate my code."*
> *"Use the `search_skills` tool to find the exact instructions for **session-wrap**."*

---

